updateChannelState.t.sol 
.
├── when contract is Paused
│   └── it REVERT - "Pausable: paused"
└── when contract is not Paused
    ├── when caller's chanel is INACTIVE or BLOCKED
    │   └── it REVERT - Core_InvalidChannel()
    └── when caller's channel is in DEACTIVATED STATE
        └── it Enter the REACTIVATION Phase
            ├── when token _amount is less than ADD_CHANNEL_MIN_FEES
            │   └── it REVERT - InvalidArg_LessThanExpected()
            └── when token _amount is accurate
                ├── it Transfer tokens from Caller to Core Contract
                ├──it Calculate and Update CHANNEL_POOL_FUNDS and PROTOCOL_POOL_FEES
                ├──it Update Channe's New Weight, Pool Contribution and Channe's State to 1 (ACTIVE STATE)
                └── it Emit ChannelStateUpdate(caller, 0, _amount)
    
