.
└── createChannelWithPUSH.t.sol
    ├── if contract is Paused
    │   └── REVERT - EnforcedPause()
    └── if contract is not Paused
        ├── if push deposit amount is less than min fees required for channel creation
        │   └── REVERT - PushCoreV2::_createChannelWithPUSH: Insufficient Deposit Amount
        └── if push deposit amount is more than equal to min fees required for channel creation
            ├── if caller has created a channel before (caller's channel state is not inactive)
            │   └── REVERT - PushCoreV2::onlyInactiveChannels: Channel already Activated
            └── if caller hasn't created a channel before (caller's channel state is inactive)
                ├── if channel type is not allowed
                │   └── REVERT - PushCoreV2::onlyUserAllowedChannelType: Invalid Channel Type
                └── if channel type is allowed
                    ├── if push token amount being transferred is greater than actually approved
                    │   └── REVERT - Push::transferFrom: transfer amount exceeds spender allowance
                    ├── Function should transfer given fees from caller account to core contract
                    └── Function should create Channel and Update Relevant State variables accordingly
                        ├── if channelType is timebound and channel expiry time is less than block.timestamp
                        │   └── REVERT - PushCoreV2::createChannel: Invalid channelExpiryTime
                        ├── Function should interact with Comm contract and make necessary Subscriptions
                        └── Function should emit AddChannelEvent with relevant params